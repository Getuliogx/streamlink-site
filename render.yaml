services:
  - type: web
    name: streamlink-site
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 1200 --workers 1 --log-level info --access-logfile - --error-logfile -
